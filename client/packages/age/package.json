{
  "name": "@vance/age",
  "version": "1.0.0",
  "private": true,
  "description": "age-encrypted documents, client side: thin wrapper around age-encryption (typage) for X25519 identities, passphrases and ASCII armor. No crypto of its own — the server never sees plaintext. Mirrors AgeDocumentKind on the Java side.",
  "license": "SEE LICENSE IN ../../LICENSE.txt",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "age-encryption": "^0.3.1"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
