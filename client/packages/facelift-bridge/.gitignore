dist/
node_modules/
*.tsbuildinfo
# The iOS and Android native projects are both committed. Their own
# ios/.gitignore and android/.gitignore exclude the generated bits
# (Pods/DerivedData/xcuserdata on iOS; build/, .gradle/, local.properties,
# copied web assets + generated config on Android).
