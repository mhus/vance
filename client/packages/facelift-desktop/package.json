{
  "name": "@vance/facelift-desktop",
  "productName": "Vancetope",
  "version": "0.1.0",
  "private": true,
  "author": "mhus",
  "description": "Vance Facelift Desktop — standalone Electron wrapper around the deployed Vance web UI (macOS/Windows/Linux). Reuses the facelift-bridge Vue shell + the VanceAccountWebView plugin interface; per-account isolation via WebContentsView + partitioned sessions.",
  "license": "SEE LICENSE IN ../../LICENSE.txt",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "tsc && electron .",
    "dist:mac": "tsc && electron-builder --mac",
    "dist:win": "tsc && electron-builder --win",
    "dist:linux": "tsc && electron-builder --linux",
    "clean": "rm -rf dist renderer *.tsbuildinfo"
  },
  "dependencies": {
    "@vance/facelift-bridge": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "43.2.0",
    "electron-builder": "^26.0.0",
    "typescript": "^5.6.0"
  }
}
