{
  "name": "@vance/runner-registry",
  "version": "1.0.0",
  "private": true,
  "description": "Shared runtime registry for document Run adapters (JS, Python, TeX, …). The host (vance-face) populates built-ins at boot; addons populate their own runners via the './register' federation expose. Cross-bundle state lives in globalThis so multiple copies of this package (host + each addon bundle) see the same Map.",
  "license": "SEE LICENSE IN ../../LICENSE.txt",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "peerDependencies": {
    "vue": "^3.5.0"
  }
}
