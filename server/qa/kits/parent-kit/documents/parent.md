# Parent Kit — Document

Shared base document, contributed by the `parent-kit` fixture. The
child kit (`test-kit`) inherits this kit, so this file lands in the
target project even though the user only imports `test-kit`.

Marker: `parent-kit-doc-v1`.
