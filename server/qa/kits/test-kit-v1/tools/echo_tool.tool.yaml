name: echo_tool
type: doc_lookup
description: "Test tool used by KitImportTest — exposes intro.md as a doc-lookup tool. The kit's only registered server-tool type today is doc_lookup; the test only verifies that the entry lands in server_tools, the tool is not actually invoked."
enabled: true
primary: false
labels:
  - test
  - doc
parameters:
  path: "intro.md"
