# Test Kit — Intro

This document is part of the `test-kit` fixture used by `KitImportTest`.
After import the brain stores it in the `documents` collection of the
target project, alongside the kit-manifest record under
`_vance/kit-manifest.yaml`.

The string `marker:test-kit-intro-v1` exists so the test can assert that
the document body round-tripped intact.
