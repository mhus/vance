/**
 * LLM tools of the Scribble addon — create/validate a handwriting sheet,
 * bootstrap a scribblebook container. Deliberately thin: an LLM cannot
 * produce usable handwriting, so there are no stroke tools.
 */
@NullMarked
package de.mhus.vance.addon.brain.scribble.tool;

import org.jspecify.annotations.NullMarked;
