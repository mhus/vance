/**
 * Cluster-related DTOs (read-only views over {@code BrainPodDocument} and
 * the small {@code /admin/ping} response). Used both by the Anus admin
 * shell and by future cluster-dashboard consumers.
 */
@org.jspecify.annotations.NullMarked
package de.mhus.vance.api.cluster;
