/**
 * Brain-side glue for the inbox subsystem — WS handlers, mappers,
 * and the answered-event listener that routes answers back to
 * waiting processes via the pending queue.
 */
@NullMarked
package de.mhus.vance.brain.inbox;

import org.jspecify.annotations.NullMarked;
