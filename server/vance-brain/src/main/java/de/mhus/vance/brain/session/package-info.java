/**
 * Brain-side session orchestration — pieces that live above the
 * persistence layer in {@code vance-shared.session} and weave engine
 * lifecycle into session create/resume.
 */
@NullMarked
package de.mhus.vance.brain.session;

import org.jspecify.annotations.NullMarked;
