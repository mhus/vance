/**
 * Tools that read the bundled Vance configuration from the classpath
 * (everything under {@code vance-defaults/}) as a structural reference.
 * Creator-only via the {@code @defaults} label selector.
 */
@NullMarked
package de.mhus.vance.brain.tools.defaults;

import org.jspecify.annotations.NullMarked;
