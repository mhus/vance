/**
 * Vogon — the deterministic multi-phase strategy engine.
 * Loads strategy plans from YAML/Mongo, runs them phase by phase
 * with worker spawns and inbox-checkpoints. See
 * {@code specification/vogon-engine.md}.
 */
@NullMarked
package de.mhus.vance.brain.vogon;

import org.jspecify.annotations.NullMarked;
