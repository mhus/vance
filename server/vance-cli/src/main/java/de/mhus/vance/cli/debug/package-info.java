/**
 * Local debug HTTP endpoints for inspecting and driving the CLI TUI from
 * automated tests or other processes. Unauthenticated — must never run with
 * {@code vance.profile=prod}.
 */
@NullMarked
package de.mhus.vance.cli.debug;

import org.jspecify.annotations.NullMarked;
