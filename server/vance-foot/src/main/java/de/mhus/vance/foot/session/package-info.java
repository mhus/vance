/**
 * Client-side session state. Owns which session is currently bound to the
 * live WebSocket and exposes that to the rest of the CLI (e.g. the prompt
 * and chat-send path).
 */
@org.jspecify.annotations.NullMarked
package de.mhus.vance.foot.session;
