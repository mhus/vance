<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
        REPL output goes through ChatTerminal to System.out / JLine writer.
        Application log lives in a file so it does not interleave with the
        chat surface. Console appender stays for WARN/ERROR so users still
        see boot-time problems before the REPL takes over.

        File path comes from Spring Boot's `logging.file.name` property if
        set (via the app-level `log-file` shim in VanceFootApplication),
        otherwise falls back to ./vance-foot.log.
    -->
    <springProperty scope="context" name="vanceLogFile" source="logging.file.name" defaultValue="vance-foot.log"/>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${vanceLogFile}</file>
        <!-- Truncate on every JVM start: the "current run's log" is the
             interesting one; persistent history is the user's job. -->
        <append>false</append>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <encoder>
            <pattern>%-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="WARN">
        <appender-ref ref="FILE"/>
        <appender-ref ref="CONSOLE"/>
    </root>
    <logger name="de.mhus.vance" level="INFO"/>
</configuration>
