/**
 * YAML-parsing and document-layer integration for the scheduler
 * subsystem. The runtime side (Spring {@code TaskScheduler}, executor,
 * overlap policy) lives in {@code vance-brain/scheduler}.
 *
 * <p>See {@code specification/scheduler.md}.
 */
@NullMarked
package de.mhus.vance.shared.scheduler;

import org.jspecify.annotations.NullMarked;
